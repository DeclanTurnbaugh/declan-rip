import { useState } from "react";
import "./Header.css";

const Header = () => {
  const [isActive, setIsActive] = useState(false);

  return (
    <header>
      <div></div>
    </header>
  );
};

export default Header;
